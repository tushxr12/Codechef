// There is also a syntax error - try and fix it to debug the code

package main
import "fmt"

func main() {
	vari := "String"
	fmt.Print(string(vari[0])) 
	fmt.Print(string(vari[1]))
	fmt.Print(string(vari[2]))
}
